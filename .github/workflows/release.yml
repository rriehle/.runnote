name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create release tarball
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          DIST_DIR="runnote-${VERSION}"

          mkdir -p "${DIST_DIR}"

          # Copy distribution files
          cp -r bin "${DIST_DIR}/"
          cp -r template "${DIST_DIR}/"
          cp -r doc "${DIST_DIR}/"
          cp config.edn LICENSE README.md CLAUDE.md "${DIST_DIR}/"

          # Create tarball
          tar czf "runnote-${VERSION}.tar.gz" "${DIST_DIR}/"

          # Create checksums
          sha256sum "runnote-${VERSION}.tar.gz" > "runnote-${VERSION}.tar.gz.sha256"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            runnote-*.tar.gz
            runnote-*.tar.gz.sha256
            install.sh
          generate_release_notes: true
          body: |
            ## Installation

            ### Quick Install
            ```bash
            curl -sL https://github.com/rriehle/.runnote/releases/download/${{ steps.version.outputs.VERSION }}/install.sh | bash
            echo 'export PATH="$HOME/.runnote/bin:$PATH"' >> ~/.bashrc  # or ~/.zshrc
            source ~/.bashrc  # or source ~/.zshrc
            ```

            ### Manual Install
            ```bash
            curl -sL https://github.com/rriehle/.runnote/releases/download/${{ steps.version.outputs.VERSION }}/runnote-${{ steps.version.outputs.VERSION }}.tar.gz | tar xz
            mv runnote-${{ steps.version.outputs.VERSION }} ~/.runnote
            echo 'export PATH="$HOME/.runnote/bin:$PATH"' >> ~/.bashrc  # or ~/.zshrc
            source ~/.bashrc  # or source ~/.zshrc
            ```

            ### GitHub Actions Usage
            See README.md for CI/CD integration examples.

            ### Verify Installation
            ```bash
            runnote-launch --help
            ```
